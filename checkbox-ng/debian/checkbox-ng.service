[Unit]
Description=Checkbox remote slave service
Wants=network.target

[Service]
ExecStart=/usr/bin/checkbox-cli slave
SyslogIdentifier=checkbox-ng.remote-slave
Environment="XDG_CACHE_HOME=/var/cache/"
Restart=on-failure
TimeoutStopSec=30
Type=simple

[Install]
WantedBy=multi-user.target
