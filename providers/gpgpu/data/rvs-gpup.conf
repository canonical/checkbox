# -*- mode: yaml -*-

# Displays the GPUs found and their properties
actions:
  - name: query
    module: gpup
    device: all
    properties:
      all:
    io_links-properties:
      all:

# vim: ft=yaml