# -*- mode: yaml -*-

# Tests for P2P compatibility and performs bandwidth tests
actions:
  - name: p2p_compat
    device: all
    module: pbqt
    log_interval: 800
    duration: 5000
    peers: all
    test_bandwidth: true
    bidirectional: true

# vim: ft=yaml
