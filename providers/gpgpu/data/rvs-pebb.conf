# -*- mode: yaml -*-

# Tests PCIe bandwidth
actions:
  - name: pci_bandwidth
    module: pebb
    device: all
    log_interval: 800
    duration: 2000
    device_to_host: false
    host_to_device: true
    parallel: false

# vim: ft=yaml
