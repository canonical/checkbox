# Validate

Validate multi-document YAML files against a JSON Schema.

This script:
- Validate given multiple schema files
- Validate yaml natively
- Validate multi document yamls

Replace this with an out of the box tool if found, I was unable to find
anything that does all 3 and is actually packaged in a sane way
